// for module main
main
'The main function is documented as follows:
call the main function through opening the program with ./program
which automatically calls the main function
Note this is actually a "relation" rather than a function but is called like a function
in neutron'
